model:
  module: models
  name: UncertaintyModel

loss:
  module: torch.nn
  name: MSELoss

optimizer:
  module: torch.optim
  name: Adam

dataset:
  module: dataloader
  name: SineDataset
  params: something here
  # more if needed

Parameters:
  batch_size: 500
  num_epochs: 30
  save_summary_steps: 1
  learning_rate: 0.001 # ?